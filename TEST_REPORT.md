# 测试与修正记录

测试日期：2026-09-03

## 已验证

- `npm test`：WebSocket 房间、多人同步、狼人私聊权限、倒计时推进与断线恢复均通过。
- `npm run lint`：通过。
- `npm run build`：通过，生产端静态资源可正常生成。
- 房间服务健康检查：测试用服务可响应 `/health`。

## 本次修正

### 房间人数校验

**问题：** 房间服务此前直接使用 WebSocket 请求中的人数。若请求传入 11、13、14 或小数等前端不支持的人数，服务端会创建没有对应角色预设的房间，可能造成房间无法正常开始或对局异常。

**修正：** 服务端现在只接受 `6、7、8、9、10、12、15` 七种整数人数；其他输入自动回退为 9 人局。

**回归测试：** 已新增测试，覆盖 `11` 与 `6.5` 两种无效输入。

### 真人联机进入同一对局

**重点验证：** 两名真人加入同一个 6 人房间，其余四席由 AI 补齐后开始游戏。

**结果：** 两名真人收到相同的 `matchId`，共享同一份服务端牌局；1、2 号真人席位在两个客户端中都保持 `isBot: false`，四个 AI 席位保持 `isBot: true`。未复现“开局后真人全部变成 AI”问题。

### 狼人独立私聊

**重点验证：** 6 名真人开局后，由服务端随机分配出的两名狼人分别发送一条消息并互相接收。

**结果：** 两名狼人均收到完整的两条对话；其余四名好人客户端的 `wolfChat` 始终为空。狼聊消息不会进入公开日志，也不会泄露给非狼人。

## 验收命令

```powershell
npm test
npm run lint
npm run build
```

## 2026-09-03 联机规则与体验升级

### 狼队协作

- 真人狼人发送“杀 1 号”“刀 1 号”“袭击 1 号”等明确目标时，AI 狼人会在私密频道回复并提交相同袭击票。
- 狼人允许投自己或狼队友。
- 狼刀按票数最多的目标结算；最高票并列时，从并列目标中随机选择。
- 狼人私聊仍只发送给存活狼人，其他阵营收到的狼聊列表始终为空。

### 全阶段倒计时与挂机处理

- 狼人、守卫、预言家、女巫阶段分别为 10 秒，超时视为跳过。
- 每名真人发言为 60 秒，超时自动过麦。
- 放逐投票新增 60 秒倒计时，超时后按已提交票数结算；无人投票则无人出局。
- 离线玩家轮到发言时立即跳过；离线玩家不会阻塞其他真人完成投票。

### 断线恢复

- 重连令牌改为保存在浏览器本地存储，同一浏览器刷新或重新打开邀请链接后可恢复原席位。
- WebSocket 意外断开后，前端每 1.2 秒自动尝试恢复连接。
- 服务端继续验证玩家 ID 与重连令牌，不能冒领已经开始的对局席位。

### 终局复盘

- 对局结束后公开所有玩家身份。
- 新增完整行动记录，包括每晚狼人袭击票、守卫目标、预言家查验、女巫用药和白天放逐票。
- 秘密行动记录只在终局发送，游戏进行中不会泄露给其他阵营。

### 美术与声音

- 使用 imagegen 生成原创哥特村庄夜景背景，并通过 Vite 资源管线打包。
- 修正头像与身份图集在 GitHub Pages 子路径部署时可能加载失败的问题。
- 新增可关闭的昼夜环境音与阶段切换提示音，使用浏览器 Web Audio 实时合成，不依赖外部音频版权资源。

### 本轮验证结果

- `npm test`：8 项全部通过。
- `npm run lint`：通过，无警告。
- `npm run build`：通过。
- 自动化覆盖 AI 狼人跟票回复、自刀、狼刀多数票/平票、真人与 AI 同局、狼人私聊隔离、离线发言跳过、投票超时、断线恢复和终局复盘。

## 2026-09-03 昼夜背景与真实音频升级

- 重新生成并接入白天、夜晚两张 16:9 原创村庄背景图，夜晚阶段使用月夜图，白天、发言、投票及终局使用晨曦图。
- 降低游戏桌遮罩透明度，修复背景虽然已打包但在界面中几乎不可见的问题。
- 删除原有 Web Audio 振荡器合成音，改为项目内置的两首 CC0 背景音乐和两个 CC0 阶段提示音。
- 所有静态音频都通过 `import.meta.env.BASE_URL` 加载，兼容 GitHub Pages 的 `/night-judgment/` 子路径。
- 音频来源与许可记录见 `AUDIO_CREDITS.md`。
