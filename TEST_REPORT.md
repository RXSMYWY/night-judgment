# 测试与修正记录

测试日期：2026-09-03

## 已验证

- `npm test`：WebSocket 房间、多人同步、狼人私聊权限、倒计时推进与断线恢复均通过。
- `npm run lint`：通过。
- `npm run build`：通过，生产端静态资源可正常生成。
- 房间服务健康检查：测试用服务可响应 `/health`。

## 本次修正

### 房间人数校验

**问题：** 房间服务此前直接使用 WebSocket 请求中的人数。若请求传入 11、13、14 或小数等前端不支持的人数，服务端会创建没有对应角色预设的房间，可能造成房间无法正常开始或对局异常。

**修正：** 服务端现在只接受 `6、7、8、9、10、12、15` 七种整数人数；其他输入自动回退为 9 人局。

**回归测试：** 已新增测试，覆盖 `11` 与 `6.5` 两种无效输入。

### 真人联机进入同一对局

**重点验证：** 两名真人加入同一个 6 人房间，其余四席由 AI 补齐后开始游戏。

**结果：** 两名真人收到相同的 `matchId`，共享同一份服务端牌局；1、2 号真人席位在两个客户端中都保持 `isBot: false`，四个 AI 席位保持 `isBot: true`。未复现“开局后真人全部变成 AI”问题。

### 狼人独立私聊

**重点验证：** 6 名真人开局后，由服务端随机分配出的两名狼人分别发送一条消息并互相接收。

**结果：** 两名狼人均收到完整的两条对话；其余四名好人客户端的 `wolfChat` 始终为空。狼聊消息不会进入公开日志，也不会泄露给非狼人。

## 验收命令

```powershell
npm test
npm run lint
npm run build
```
